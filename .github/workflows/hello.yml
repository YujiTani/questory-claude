name: Hello # ワークフロー名
on: push # イベント（この場合push時に起動）
jobs: # ジョブ定義
  hello: # ジョブID
    runs-on: ubuntu-latest # ランナー（ubuntuで実行）
    steps: # ステップの定義
      - run: echo "Hello", "World" # シェルコマンド実行
      - uses: actions/checkout@v4 # アクション呼び出し
